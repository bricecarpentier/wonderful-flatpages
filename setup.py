from setuptools import setup

setup(
    name="wonderful-flatpages",
    version="0.1.0",
    packages=[
        'flatpages_placeholders',
    ],
    install_requires=[
        "django>=1.5",
    ]
)
